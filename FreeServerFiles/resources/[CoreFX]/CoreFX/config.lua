config = {}

config.options = {
	-- Makes the siren lights brighter for emergency vehicles
	BrighterEmergencyLights = false,

	-- Changes up the color of street lights with whiter ones (might conflict with some custom mlo maps)
	CoffeeStreetLights = false,

	-- Removes the corona effect from street lights & other light sources
	RemoveCorona = false
}